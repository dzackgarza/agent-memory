# Implementation Diff Audit

Target reader: a future agent checking whether implementation stayed inside the frozen plan and owned-surface ledger.

Reader task: classify the current changed surface against the proof workflow plan.

## File Classification

| Path | Classification | Plan anchor |
| --- | --- | --- |
| `src/iwe2/__init__.py` | Planned production code | Package exports from `planning/implementation-plan.md`. |
| `src/iwe2/__main__.py` | Planned production code | Executable module entrypoint from `planning/implementation-plan.md`. |
| `src/iwe2/cli.py` | Planned production code | Cyclopts command surface from `planning/implementation-plan.md`. |
| `src/iwe2/models.py` | Planned production code | Strict config/frontmatter contracts from `planning/implementation-plan.md`. |
| `src/iwe2/operations.py` | Planned production code | Filesystem, Git, IWE, `rg`, Probe, and `zk` orchestration from `architecture/owned-surface-ledger.md`. |
| `src/iwe2/py.typed` | Planned packaging artifact | Typed Python package marker for the declared Python project. |
| `tests/test_cli_workflows.py` | Planned proof tests | CLI integration tests from `testing/test-plan.md`. |
| `pyproject.toml` | Planned project config | Python package, dependencies, and console script surface. |
| `uv.lock` | Planned lockfile | uv-managed dependency resolution for the Python package. |
| `justfile` | Planned QC delegation | Delegates `test` and `test-ci` to global Python QC. |
| `.gitignore` | Planned repository hygiene | Excludes local caches and environments generated by tooling. |
| `tool-artifacts/.gitkeep` | Planned artifact directory marker | Keeps the repository artifact directory available. |
| `README.md` | Planned user entry point | Points to the design transcript and proof workflow. |
| `.agents/proof-obligation-workflow/**` | Planned proof artifacts | Requirements-to-proof workflow state. |
| `DESIGN-TRANSCRIPT.md` | Canonical source material | User-provided design source, formatted by global text formatting. |

## Scope Findings

| Check | Result | Evidence |
| --- | --- | --- |
| Unplanned production modules | None found | Production code is limited to `src/iwe2/`. |
| Suspicious test edits | None found | Tests are public CLI subprocess workflows with real filesystem, Git, IWE, `rg`, Probe, and `zk` boundaries. |
| Hidden test branches | None found | Serena symbol overview shows tests call the installed CLI through helpers, not production test modes. |
| Local QC authority | None added | The top-level `justfile` delegates to `~/ai-review-ci`; no local generic tool config was added. |
| Bespoke graph engine | None added | IWE owns graph layout, retrieval, and refactor mechanics; wrapper code owns scope and policy glue. |
| Custom search index | None added | Title/key graph search uses IWE `find`; body search uses `rg` over selected vault roots; indexed search delegates to `zk`. |

## Owned-Surface Budget

The implementation remains inside the accepted ownership split: local code owns CLI composition, strict models, memory-specific filesystem layout, project bootstrap, promotion policy, scope root selection, graph anchor selection, and dependency result normalization.
IWE, Git, `rg`, Probe, and `zk` own graph behavior, repository identity, title/key graph search, lexical search, ranked context search, and indexed Markdown search.

No implementation-diff issue requires rerouting to architecture minimization.
